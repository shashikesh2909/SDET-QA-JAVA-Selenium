package Day9;

import java.util.Scanner;

public class StringArrayExamples {

	public static void main(String[] args) 
	{
		
		Scanner sc = new Scanner(System.in);
	//	System.out.println("Enter the string to be reversed");
	//	String s = sc.next();

/*
		// Reverse a string 
		// Approach 1 : using charAt() & s.length()                         there is no method to directly reverse the string
		
		String rev ="";
		for(int i=s.length()-1;i>-1;i--)
		{
			rev = rev + s.charAt(i);                       
		}
		System.out.println(rev);

		
	//	Approach 2 : converting string into char array.
		
		char a[] = s.toCharArray();
		String rev = "";
		
		for ( int i =a.length-1;i>-1;i--)
		{
			rev = rev+a[i];
		}
		System.out.println(rev);

		
		// Approach 3 :  using string buffer class
		
		StringBuffer a = new StringBuffer("welcome");
		System.out.println(a.reverse());                                // string Buffer is class where we can create an object of a BufferString:   reverse() . method can reverse the StringBuffer object
	
		
		// Approach 3 : using string builder class
		StringBuilder a = new StringBuilder("welcome");
		System.out.println(a.reverse()); 
*/		
		
// 	String Comparison 
		
		
		
		
	}

}

